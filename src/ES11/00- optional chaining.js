const usuario = {
    redes: {
        facebook:{
            direccion: '@sebas.dot'
        }

    }
}

console.log(usuario.redes.instagram?.facebook);

// ayuda a hacer peticiones que posiblmente no existan y no dañen la aplicacion
